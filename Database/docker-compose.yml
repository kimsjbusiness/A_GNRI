version: '3.8'

services:
  a_gnri_database:
    image: postgres:15
    container_name: A_GNRI_DB
    restart: always
    ports:
      - "25725:5432"
    environment:
      POSTGRES_USER: admin_ksj
      POSTGRES_PASSWORD: qlalfqjsgh1234
    volumes:
      - ~/postgres_data:/var/lib/postgresql/data

networks:
  default:
    name: ksj_project_network